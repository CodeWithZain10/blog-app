<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title><%= blog.title %></title>
</head>
<body class="bg-gray-100">
  <!-- Header -->
  <div class="w-full mx-auto py-6 px-4 sm:px-6 lg:px-8">
    <div class="bg-white shadow overflow-hidden sm:rounded-lg">
      <div class="px-4 py-5 sm:px-6 flex justify-between items-center">
        <div>
          <h1 class="text-3xl font-bold text-gray-900"><%= blog.title %></h1>
          <p class="mt-1 text-sm text-gray-500">Published on <%= new Date(blog.date).toLocaleDateString() %></p>
        </div>

      </div>

  <!-- Blog Content -->
  <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
    <div class="bg-white shadow overflow-hidden sm:rounded-lg">
      <div class="px-4 py-5 sm:px-6">
        <% if (blog.image) { %>
          <img class="w-full h-[550px] rounded-lg mb-6" src="data:image/jpeg;base64,<%= blog.image.toString('base64') %>" alt="Blog picture">
        <% } %>
        <div class="text-lg leading-7 text-gray-700">
          <p><%= blog.content %></p>
        </div>
      </div>

      <div class="border-t border-gray-200 px-4 py-5 sm:px-6">
        <h2 class="text-2xl font-bold text-gray-900">About the Author</h2>
        <div class="flex items-center mt-4">
          <% if (blog.user.profileImg) { %>
            <img class="h-12 w-12 rounded-full" src="data:image/jpeg;base64,<%= blog.user.profileImg.toString('base64') %>" alt="Author picture">
          <% } else { %>
            <img class="h-12 w-12 rounded-full" src="/path/to/default/profile/pic.jpg" alt="Author picture">
          <% } %>
          <div class="ml-4">
            <div class="text-lg font-medium text-gray-900"><%= blog.user.username %></div>
            <div class="text-sm text-gray-500">Published on <%= new Date(blog.date).toLocaleDateString() %></div>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-white shadow">
    <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
      <p class="text-center text-gray-500">&copy; <%= new Date().getFullYear() %> Blogger. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>
